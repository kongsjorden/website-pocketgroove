---
const navLinks = [
  { href: '/features', label: 'Features' },
  { href: '/faq', label: 'FAQ' },
  { href: '/support', label: 'Support' },
];

const currentPath = Astro.url.pathname;
---

<header class="fixed top-0 left-0 right-0 z-50 bg-background/80 backdrop-blur-xl border-b border-border/50">
  <nav class="max-w-6xl mx-auto px-6 h-16 flex items-center justify-between">
    <!-- Logo -->
    <a href="/" class="flex items-center gap-3 group">
      <div class="w-10 h-10 rounded-xl bg-primary flex items-center justify-center transition-transform duration-300 group-hover:scale-105 group-hover:rotate-3">
        <svg class="w-6 h-6 text-white" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <circle cx="12" cy="12" r="9"/>
          <line x1="12" y1="12" x2="12" y2="7" stroke-linecap="round"/>
          <line x1="12" y1="12" x2="16" y2="12" stroke-linecap="round"/>
          <circle cx="12" cy="12" r="1" fill="currentColor"/>
        </svg>
      </div>
      <span class="font-display font-bold text-xl text-text tracking-tight">
        Pocket<span class="text-primary">Groove</span>
      </span>
    </a>

    <!-- Desktop Navigation -->
    <div class="hidden md:flex items-center gap-1">
      {navLinks.map(link => (
        <a
          href={link.href}
          class:list={[
            "px-4 py-2 rounded-lg text-sm font-medium transition-all duration-200",
            currentPath === link.href
              ? "bg-surface text-text"
              : "text-text-muted hover:text-text hover:bg-surface/50"
          ]}
        >
          {link.label}
        </a>
      ))}
      <span
        class="ml-4 px-5 py-2.5 bg-primary/50 text-white/70 text-sm font-semibold rounded-lg cursor-default"
      >
        Coming Soon
      </span>
    </div>

    <!-- Mobile Menu Button -->
    <button
      id="mobile-menu-btn"
      class="md:hidden p-2 rounded-lg hover:bg-surface transition-colors"
      aria-label="Toggle menu"
    >
      <svg class="w-6 h-6 text-text" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path id="menu-icon" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/>
      </svg>
    </button>
  </nav>

  <!-- Mobile Menu -->
  <div id="mobile-menu" class="md:hidden hidden bg-surface border-t border-border">
    <div class="px-6 py-4 space-y-2">
      {navLinks.map(link => (
        <a
          href={link.href}
          class:list={[
            "block px-4 py-3 rounded-lg text-base font-medium transition-colors",
            currentPath === link.href
              ? "bg-background text-text"
              : "text-text-muted hover:text-text hover:bg-background/50"
          ]}
        >
          {link.label}
        </a>
      ))}
      <span
        class="block mt-4 px-4 py-3 bg-primary/50 text-white/70 text-center font-semibold rounded-lg cursor-default"
      >
        Coming Soon
      </span>
    </div>
  </div>
</header>

<script>
  const menuBtn = document.getElementById('mobile-menu-btn');
  const mobileMenu = document.getElementById('mobile-menu');

  menuBtn?.addEventListener('click', () => {
    mobileMenu?.classList.toggle('hidden');
  });
</script>

<style>
  .font-display {
    font-family: 'Outfit', sans-serif;
  }
</style>

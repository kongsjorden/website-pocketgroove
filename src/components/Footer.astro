---
const currentYear = new Date().getFullYear();

const footerLinks = {
  product: [
    { href: '/features', label: 'Features' },
    { href: '/faq', label: 'FAQ' },
  ],
  support: [
    { href: '/support', label: 'Get Help' },
    { href: 'https://github.com/kongsjorden/website-pocketgroove/discussions', label: 'Discussions', external: true },
    { href: 'https://github.com/kongsjorden/website-pocketgroove/issues', label: 'Report Bug', external: true },
    { href: 'mailto:kongsjorden@gmail.com', label: 'Email', external: true },
  ],
  legal: [
    { href: '/privacy', label: 'Privacy Policy' },
  ],
};
---

<footer class="bg-surface border-t border-border mt-auto">
  <div class="max-w-6xl mx-auto px-6 py-12">
    <div class="grid grid-cols-2 md:grid-cols-4 gap-8 mb-12">
      <!-- Brand -->
      <div class="col-span-2 md:col-span-1">
        <a href="/" class="flex items-center gap-2 mb-4">
          <div class="w-8 h-8 rounded-lg bg-primary flex items-center justify-center">
            <svg class="w-5 h-5 text-white" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <circle cx="12" cy="12" r="9"/>
              <line x1="12" y1="12" x2="12" y2="7" stroke-linecap="round"/>
              <line x1="12" y1="12" x2="16" y2="12" stroke-linecap="round"/>
            </svg>
          </div>
          <span class="font-display font-bold text-lg text-text">PocketGroove</span>
        </a>
        <p class="text-text-muted text-sm leading-relaxed">
          Rhythm training for musicians. Tap. Listen. Improve.
        </p>
      </div>

      <!-- Product Links -->
      <div>
        <h4 class="font-display font-semibold text-text mb-4">Product</h4>
        <ul class="space-y-3">
          {footerLinks.product.map(link => (
            <li>
              <a href={link.href} class="text-text-muted hover:text-primary text-sm transition-colors">
                {link.label}
              </a>
            </li>
          ))}
        </ul>
      </div>

      <!-- Support Links -->
      <div>
        <h4 class="font-display font-semibold text-text mb-4">Support</h4>
        <ul class="space-y-3">
          {footerLinks.support.map(link => (
            <li>
              <a
                href={link.href}
                class="text-text-muted hover:text-primary text-sm transition-colors inline-flex items-center gap-1"
                target={link.external ? '_blank' : undefined}
                rel={link.external ? 'noopener noreferrer' : undefined}
              >
                {link.label}
                {link.external && (
                  <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"/>
                  </svg>
                )}
              </a>
            </li>
          ))}
        </ul>
      </div>

      <!-- Legal Links -->
      <div>
        <h4 class="font-display font-semibold text-text mb-4">Legal</h4>
        <ul class="space-y-3">
          {footerLinks.legal.map(link => (
            <li>
              <a href={link.href} class="text-text-muted hover:text-primary text-sm transition-colors">
                {link.label}
              </a>
            </li>
          ))}
        </ul>
      </div>
    </div>

    <!-- Bottom Bar -->
    <div class="pt-8 border-t border-border flex flex-col md:flex-row justify-between items-center gap-4">
      <p class="text-text-dim text-sm">
        &copy; {currentYear} Kongsjorden Studio. All rights reserved.
      </p>
    </div>
  </div>
</footer>

<style>
  .font-display {
    font-family: 'Outfit', sans-serif;
  }
</style>
